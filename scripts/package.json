{
    "name": "firestore-export",
    "version": "1.0.0",
    "description": "Export Firestore data to JSON for migration",
    "type": "module",
    "scripts": {
        "export-firestore-to-json": "npx tsx export-firestore.ts"
    },
    "dependencies": {
        "firebase-admin": "^12.0.0"
    },
    "devDependencies": {
        "tsx": "^4.7.0",
        "typescript": "^5.3.0"
    }
}